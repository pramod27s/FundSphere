spring.application.name=CoreBackend
spring.datasource.url=jdbc:postgresql://localhost:5432/FundSphere
spring.datasource.password=root3456
spring.datasource.username=postgres
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Shared API key used by internal CoreBackend <-> ai-service calls.
integration.api-key=ppramodwejfbewiufgewiuf

# CoreBackend -> ai-service bridge settings.
integration.ai.base-url=http://localhost:8000
integration.ai.api-key=${integration.api-key}
integration.ai.connect-timeout-ms=3000
integration.ai.read-timeout-ms=20000

