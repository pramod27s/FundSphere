/*
Generate a Spring Boot REST API for managing scraped grant data in PostgreSQL.

Project Context:
This backend is part of the FundSphere system. A Python FastAPI service performs web scraping and LLM-based attribute extraction of grant pages. FastAPI will call these Spring Boot APIs to store and manage grant data in PostgreSQL.



1. Create an Entity class called Grant with the following fields:

- id (Long, primary key)
- grantTitle
- fundingAgency
- programName
- description
- grantUrl
- applicationDeadline
- fundingAmountMin
- fundingAmountMax
-funding Currency
-eligibleCountries
-EligibleApplicants
-institutionType
-field
-application_link
- checksum (String) -> used to detect if the grant page content has changed
- tags (List<String>) -> used for fast searching and filtering
- createdAt
- updatedAt
- lastScrapedAt

Use @ElementCollection for storing tags.

2. Create a GrantRepository using JpaRepository.

3. Create a Service layer called GrantService with the following methods:

- saveOrUpdateGrant(Grant grant)
- getAllGrants()
- getGrantById(Long id)
- updateGrant(Long id, Grant grant)
- deleteGrant(Long id)
if more needed then add


Important Logic:
When FastAPI sends a grant to store:

Step 1:
Check if a grant with the same grantUrl already exists.

Step 2:
If it does not exist:
→ Save it as a new record.

Step 3:
If it exists:
Compare the checksum.

If checksum is the same:
→ Do nothing (grant page has not changed).

If checksum is different:
→ Update the grant record
→ Update checksum
→ Update updatedAt timestamp.

4. Create a REST controller called GrantController with endpoints:

POST /api/grants
Used by FastAPI to store scraped grant data.

GET /api/grants
Return all stored grants.

GET /api/grants/{id}
Return a specific grant.

PUT /api/grants/{id}
Update grant.

DELETE /api/grants/{id}
Delete grant.
if moore needed then addd



5. The API should accept and return JSON.

6. Add basic exception handling if a grant is not found.

7. Add CORS configuration so the FastAPI service can call these APIs.



Generate the full code for:
- Entity
- Repository
- Service
- Controller
*/